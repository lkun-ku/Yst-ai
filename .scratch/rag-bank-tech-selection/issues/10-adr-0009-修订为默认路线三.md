# 10: ADR-0009 修订为默认路线③

- Status: ready-for-agent
- Type: task
- Blocked by: 07
- 关联：`docs/adr/0009-知识库出题编排框架引入裁决.md`、issue `07-用户决策采纳路线三.md`

## What to build

将 ADR-0009 的决策由"默认路线②、路线③备用"精细化修订为"**默认路线③（LangGraph 编排）、路线②保留可选**"，并注明系用户直接决策；同步 Considered Options / Consequences，原"越阈值优先③"条款因已为默认而失效。保留"不单独引入 LangChain 检索层"的约束。

## 验收清单

- [x] Decision 改为默认路线③、路线②保留可选，标注用户直接决策。
- [x] Considered Options / Consequences 同步（原推荐"路线②默认"调整为未采纳）。
- [x] 依赖 `langgraph==1.2.11` + `langchain-core==1.6.2` 已钉 `requirements.txt` 并验证兼容。

## 状态

已落地（历史追溯）：2026-05-25 完成。ADR-0009 已反映最终决策。
