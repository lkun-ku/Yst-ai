# 08: 路由切换默认路线③

- Status: ready-for-agent
- Type: task
- Blocked by: 07
- 关联：`backend/app/routers/kb.py`、ADR-0009、issue `07-用户决策采纳路线三.md`

## What to build

将 HTTP 入口默认调度切到 LangGraph，**对外行为不变**。在 `POST /api/kb/generate` 新增 `route` 字段（默认 `"graph"`），经纯函数 `_select_generator(route)` 分发到路线③；`route="handwritten"` 走路线②；非法值返回 400。其余检索 / 提示词 / LLM / 校验 / 落库逻辑零改动——维持"控制变量"原则，确保实验结论与质量闭环降级行为不被破坏。

## 验收清单

- [x] 默认 `route="graph"` 调 `kb_graph.generate_by_scope_graph`（路线③）。
- [x] `route="handwritten"` 仍走 `kb_generate.generate_by_scope`（路线②）。
- [x] 非法 `route` 经入口返回 400（白名单 `VALID_ROUTES`）。
- [x] 共享逻辑无回归，全量 `pytest backend/tests` 通过。

## 状态

已落地（历史追溯）：2026-05-25 完成。`kb.py` 的 `route` 字段 + `_select_generator` 分发 + 无效值 400 就绪。
