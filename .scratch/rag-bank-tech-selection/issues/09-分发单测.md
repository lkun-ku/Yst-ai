# 09: 分发单测

- Status: ready-for-agent
- Type: task
- Blocked by: 08
- 关联：`backend/tests/test_kb_dispatch.py`、`backend/app/routers/kb.py`

## What to build

用离线单测锁定路由分发行为，确保"默认③、②可选、非法 400"不被回归破坏，且不依赖真实 embedding / LLM / 数据库：

- `_select_generator("graph")` 取 LangGraph 生成函数。
- `_select_generator("handwritten")` 取手写生成函数。
- 非法 `route` 经端点返回 400。

分发选择抽成纯函数，使单测无需真实 DB 会话；400 用例走真实端点（含 `get_db` / `get_current_candidate` 依赖覆盖）。

## 验收清单

- [x] `test_kb_dispatch.py` 覆盖默认③ / ②可选 / 非法 400 三个断言。
- [x] 单测离线可跑（mock 模块层生成函数，无真实 LLM/DB）。
- [x] 全量 `pytest backend/tests` 通过，无 teardown 报错。

## 状态

已落地（历史追溯）：2026-05-25 完成。分发单测稳定通过。
