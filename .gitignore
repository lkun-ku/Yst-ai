# Python

__pycache__/
*.py[cod]
.venv/
venv/
backend/.test_tmp.db
backend/.test_tmp.db-journal
backend/dev.db
backend/uploads/

# Node / Taro
node_modules/
miniprogram/dist/
miniprogram/.temp/
miniprogram/.rn_temp/
miniprogram/.swc/

# Env（密钥不入库）
.env
.env.local
backend/.env

# OS
.DS_Store

# 评测产物（2026-06-11 调整）：**基准表入库** —— `results/*.md|json` 是
# 「某项优化到底有没有效」的唯一证据；不随代码走就等于没有证据（面试时只能口述数字）。
# 评测库仍是本地重算的中间产物（每次运行都会重建），不入库。
backend/eval/eval_kb.db
backend/eval/eval_kb.db-journal
# 基准脚本的**后台运行日志**（一次性产物，2026-06-11）：跑长基准要放后台，
# 日志就落在 results/ 里。只删一次文件不够 —— 跑一次就会重新生成，故写进忽略规则。
backend/eval/results/.t_run.*

# 上 GitHub 前清理（#40）：IDE 数据 / 旧版目录 / 含密钥的本地脚本
.codebuddy/
miniprogram/
.scratch/
启动后端-real.bat
一键冒烟测试.bat
# docs/ 已解除忽略（2026-06-10）：计划书 / ADR / 用户需求文档必须进仓库 ——
# 否则「归档」与「删除」都无法从 git 恢复（已因此永久丢过一份决策记录）。
# 目录约定见 docs/README.md：现行文档在顶层，历史文档在 docs/archive/，学习材料在 docs/learning/。

# 过程产物与实验内容（本地保留，不入仓库）
ui-concepts/
# 精排权重（2026-06-11）：266MB，不随仓库走。下载：python -m app.services.rerank --fetch
# 缺权重时检索自动降级为「不重排」（fail-open），不会因此失败。
backend/data/models/
# backend/eval/ 已解除忽略（2026-06-10）：评测集 datasets/ 是量化证据，必须进仓库；
# 实验产物与评测库仍忽略（见上方「评测产物」段）。

# AGENTS.md / CONTEXT.md 已解除忽略（2026-06-10）：
# CONTEXT.md 是「术语单一真相」，AGENTS.md 是协作约定 ——
# 被忽略会导致「新会话读不到约定」与「术语表不进仓库、随本机丢失」。

# 微信开发者工具本地私有配置（各开发者独立，含本地调试设置，不入仓库）
project.private.config.json
miniprogram-native/project.private.config.json
