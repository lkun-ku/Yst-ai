# `docs/eval/` —— 基准快照与「承诺 ↔ 证据」索引

> **分工**：`backend/eval/results/` 放**原始产物**（每次跑就新增/覆盖，含运行时间与参数）；
> 本目录放**需要长期对照的基准**，以及这张**索引表**。
>
> 索引为什么必要：§6 的承诺表写的是「目标」，而数字散在十几个证据文件里 ——
> **没有这张表，"某条承诺到底达标没有"只能靠人去翻**，于是很容易一直停在"应该没问题"。

## 承诺 ↔ 证据（对齐日期 **2026-06-15**）

| §6.3 指标 | 目标 | 证据文件 | 实测 | 判定 |
| --- | --- | --- | --- | --- |
| Recall@K | ≥ 0.80 @5 | `backend/eval/results/retrieval_baseline_官方法条.json`（415 片 / 135 查询）、`retrieval_baseline_教资.json`（15 查询） | **只测到 K = 1/2/3**：生产档 `@1 = 0.3111`；精排档 `@1 = 0.8000`、`@3 = 0.9407` | ⚠️ **口径不一致**，@5 无法对照 |
| MRR | ≥ 0.60 | 同上 | 生产档 **0.4565**；精排档 0.8753 | ❌ **未达标**（生产档；精排默认关闭） |
| 引用首次通过率 | ≥ 0.90 | `teacher_baseline.json` | **1.0**（grounded / agent，n=20 可答） | ✅（样本小） |
| 拒答率 | 10% ~ 25% | `teacher_baseline.json` | **20%**（5/25 不可答，全部正确拒答） | ✅（不可答集为人工设计） |
| 事实性 faithfulness | ≥ 0.90 | **（无）** | — | ❌ **无证据** |
| 唯一性通过率 | ≥ 0.90 | `g3_baseline.json` / `g3_真题单选_baseline.json` / `g3_n2_*` / `g3_n3_*` | 实测的是**误杀率**（0.0 / 0.05 / 3.78%）与**负样本拦截率**（1.0 / 0.75 / 0.15） | ❌ **测的不是这个指标** |
| 评分一致性 | 一致率 ≥ 0.80；维度标准差 ≤ 10 | `marking_baseline.json`（7 题 × 3 遍） | 一致率 **0.3333**、最大维度标准差 **13.59** | ❌ **未达标**（两口径同指写作） |
| 成本（调用次数） | 平均 ≤ 2、P95 ≤ 4 | `teacher_baseline.json` | 平均 **1.8 / 1.2**（两种模式）；**P95 无记录** | ⚠️ 部分达标 |

## §6.2 产品指标

| 指标 | 目标 | 现状 |
| --- | --- | --- |
| 卷面覆盖率 | ≥ 95% | **能力上 100%**（39% 单选 + 28% 材料分析 + 33% 写作），但为**推算值**、非实测 |
| 主观题批改完成率 | ≥ 95% | 链路已落地（`routers/marking.py`），**未做线上统计** |
| 北极星 / 次周留存 / 诊断命中率 | — | **未上线，无数据** |

## 三条读数的纪律（踩出来的）

1. **换了输入，旧基线就不再是基线** —— 批改依据从"纯官方语料"改成"答案库（同题型）优先 + 官方兜底"之后，
   上面那张一致性表**不能与新数字直接比较**，需要在新依据下重测（等 LLM 额度恢复）。
2. **样本变厚，结论可能翻转** —— 评分一致性的旧基线（2 题）是"达标"，扩到 7 题后变"未达标"。
   「达标」这两个字在 n=2 上没有意义。
3. **"测了"不等于"测的是那条指标"** —— 唯一性通过率这一行就是例子：有一堆 G3 证据，
   但它们测的是误杀率/拦截率，**没有一条**是"生成题目通过 G3 的比例"。指标名与测量口径必须逐字对上。
