# 阶段一·术语裁决记录

> 依据总方针「代码即事实」：文档与代码冲突时，以当前代码实际行为为准，修正文档（不擅自改代码迁就旧文档）。
> 每条记录四要素：冲突点 / 文档说法 / 代码现状 / 裁决。
> 本记录对应 `CONTEXT.md` 阶段一第 1 步（`/domain-modeling`）的术语校准。

## D1 模块(Module) 是否含「个人资料」
- **冲突点**：`CONTEXT.md` 的「模块」只列考纲五维，未提 `PERSONAL`。
- **文档说法**：模块 = 职业理念/职业道德/教育法律法规/文化素养/基本能力。
- **代码现状**：`models.Module` 含 6 值，`PERSONAL="个人资料"`，且 `OFFICIAL_MODULES` 显式排除 `PERSONAL`；掌握度/复盘统计不计入官方五维。
- **裁决**：以代码为准。`CONTEXT` 增补 `PERSONAL` 为第六维，标注「官方模块」仅指前五维。

## D2 题型枚举缺失
- **冲突点**：`CONTEXT.md` 有「题型配比」但无题型取值清单。
- **文档说法**：仅「题型配比」概念。
- **代码现状**：`QuestionType = single/multiple/judge/blank/short`，各带明确语义（blank 归一化匹配、short 不自动判分）。
- **裁决**：以代码为准。`CONTEXT` 增补「题型」术语并列出五种取值。

## D3 出题任务分裂为单文档 / 跨文档
- **冲突点**：`CONTEXT.md` 只有笼统「出题任务」。
- **文档说法**：出题任务 = 一次资料出题异步单元。
- **代码现状**：`DocTask`（绑单文档，paper/spot 两模式）与 `KbTask`（跨多份个人资料，按 scope）两张表、两种轮询。
- **裁决**：以代码为准。`CONTEXT` 拆为「文档出题任务(DocTask)」「知识库出题任务(KbTask)」。

## D4 两套额度混为一谈
- **冲突点**：`CONTEXT.md` 只有「每日额度（作答题量）」。
- **文档说法**：每日额度按题量限速。
- **代码现状**：另有 `RealtimeUsage`（实时生成每日 3 次，变式+复盘共用，超限降级纯池化）。
- **裁决**：以代码为准。`CONTEXT` 增补「实时生成额度」并区分于作答额度。

## D5 纠错与审校未区分
- **冲突点**：`CONTEXT.md` 只有「审校」（入库前）。
- **文档说法**：审校 = 入库前检查。
- **代码现状**：`ErrorReport` 是入库后考生反馈（答案/解析/考点归属），进审校队列处理。
- **裁决**：以代码为准。`CONTEXT` 增补「纠错」并区分入库前/后。

## D6 溯源(source_chunk)未定义
- **冲突点**：`CONTEXT.md` 有「切片」但无题目级溯源概念。
- **文档说法**：切片是检索最小单位。
- **代码现状**：`Question.source_chunk` 存生成所依切片 id 清单，越界回退批级清单防幻觉。
- **裁决**：以代码为准。`CONTEXT` 增补「溯源」术语。

## D7 产品定位：泛化为通用 RAG 出题工具（已裁决）
- **冲突点**：`CONTEXT.md` 标题/导语称「教资《综合素质》方向」，但其「知识库」定义已写「出题不再限定于教资范畴」，代码 `Module.PERSONAL` 也支持任意领域资料出题。
- **两种说法**：A) 仍定位教资刷题工具；B) 泛化为「任意资料→出题」通用 RAG 工具；C) 两者并存分轨。
- **代码现状**：技术上已支持任意领域（PERSONAL 维度 + scope 自然语言跨文档出题）。
- **裁决（用户 2026-05-26）**：采用 **B) 泛化为通用 RAG 出题工具**。教资《综合素质》作为首批落地内容领域之一，产品范围不限定于教资。
- **理由**：代码能力已跨领域；导语与「知识库」定义自相矛盾；以通用工具定位才能覆盖"用户上传任意资料出题"的真实用法，避免定位窄化限制 RAG 路线。
- **落地**：`CONTEXT.md` 导语已重写；新 spec 范围全面泛化，教资列为首批内容。

---

## 阶段一·第 2 步 文档矛盾裁决（/grill-with-docs，C1–C4）

> 第一批 4 条，用户于 2026-05-26 逐条裁决。按「代码即事实」总方针，doc-vs-code 项以代码为准。

### C1 ｜ ADR-0010 双列 embedding vs 代码单列
- **冲突点**：ADR-0010 Decision 1 写「新增 `embedding_vec` 双列（不替换原 `embedding` BLOB）」。
- **文档 A 说法**：ADR-0010 —— 双列并存。
- **代码现状**：`models.py` `DocumentChunk.embedding` 为**单列** `Vector(1024).with_variant(LargeBinary,"sqlite")`；W-19 已删除 `embedding_vec`、合并回单列。
- **裁决（用户：同意建议）**：以代码为准。修订 ADR-0010 增加「实施更新」说明，Decision 1 标注双列已合并取消。

### C2 ｜ ADR-0005「删除 Taro」vs 仓库残留 `miniprogram/`
- **冲突点**：ADR-0005 Decision 写「删除 Taro」，但 `miniprogram/`（Taro+Vue3）目录仍在仓库。
- **文档 A 说法**：ADR-0005 —— 删除 Taro。
- **代码/仓库现状**：根目录 `miniprogram/` 仍存在，构成技术债与混淆源。
- **裁决（用户：清理）**：执行清理——删除 `miniprogram/` 目录（通过 `git rm -r` 移除，保留于 git 历史可恢复）。

### C3 ｜ `提交历史.md` 滞后到 #25 vs 实际 HEAD
- **冲突点**：提交历史索引仅到 #25（`d15c426`/30ea652），HEAD 已到 `257407b`，中间 11 个提交未补录。
- **文档 A 说法**：`docs/提交历史.md` 表格止于 #25。
- **代码现状**：`git log` 显示 09-06→09-10 另有 11 个提交（原生重写、RAG 出题、kb 页等）。
- **裁决（用户：补录）**：补录 #26–#36 十一条记录（表格 + 详情）。

### C4 ｜ `需求分析文档`「教资限定」vs 知识库定义 / D7 泛化裁决
- **冲突点**：`需求分析文档.md` 以教资《综合素质》为范围、并砍掉「泛知识自由输入」（W4）；但其下游「知识库」定义已写「任意领域」，且 D7 裁决泛化为通用 RAG 出题工具。
- **文档 A 说法**：`需求分析文档.md` —— 教资垂直、W4 砍掉泛知识。
- **文档 B 现状**：`CONTEXT.md` 知识库定义 + D7 裁决 = 通用 RAG 出题工具，教资仅首批内容。
- **裁决（用户：同意）**：在 `需求分析文档.md` 顶部加「定位变更提示」横幅，并标注 W4 被 D7 推翻、Q9/Q17 教资专属表述待新 spec 重定；新 spec 以泛化定位为准。

---

## 阶段一·第 2 步 文档矛盾裁决（第二批，B1–B5）

> 第二批 5 条，用户于 2026-05-26 逐条裁决。doc-vs-code 项以代码为准；B2 为产品方向裁决。

### B1 ｜ 技术方案 v2 仍写 `embedding_vec` 双列 vs 代码单列
- **冲突点**：v2 第 109 行 `embedding_vec VECTOR(1024)` 双列表述（与 C1 同源）。
- **文档 A**：v2 —— 双列。
- **代码现状**：`models.py` 单列 `embedding`。
- **裁决（用户：同意）**：以代码为准，v2 第 109 行改为单列 `embedding`（已修订）。

### B2 ｜ 「完全免费不做 VIP」vs 代码已实现 VIP + 需求分析 S4 要 VIP 差异化
- **冲突点**：v2 第 12 行「完全免费，不做 VIP（偏离原 S4）」；代码已实现 VIP 权益页 + `vip-activate`（ADR-0001 内测占位）；需求分析 S4 要「VIP 差异化」。
- **文档 A**：v2 —— 不做 VIP。
- **文档 B / 代码**：需求分析 S4 要 VIP 差异化；代码有 VIP 骨架。
- **裁决（用户：完全免费）**：产品定为**完全免费、不做 VIP**。v2 已与该方向一致（无需改）；需求分析 S4 及相关 VIP 表格/商业化章节需标注被 B2 推翻；**代码侧 VIP 骨架（权益页、`quota.vip-activate`、ADR-0001 占位）现与裁决冲突，登记为待清理工单（阶段二/三执行），阶段一不直接删代码**。

### B3 ｜ v2 B5「多文档出卷留二期」vs 代码已跨文档
- **冲突点**：v2 第 35 行 B5 多文档留二期。
- **文档 A**：v2 —— 二期。
- **代码现状**：W-5/W-6 跨文档出卷已落地。
- **裁决（用户：同意）**：以代码为准，v2 B5 改为已实现（已修订）。

### B4 ｜ v2 S4「RAG 主体未实现」vs 代码已落地
- **冲突点**：v2 第 99 行 S4 主体未实现。
- **文档 A**：v2 —— 未实现。
- **代码现状**：RAG 出题全链路 + 路线③默认已落地。
- **裁决（用户：同意）**：以代码为准，v2 S4 改为已实现（已修订）。

### B5 ｜ MVP spec 写 Taro/ADR-0004/RAG=Should vs 代码已原生+跨文档 RAG
- **冲突点**：spec 第 117/259 行 Taro/ADR-0004；第 216 行 RAG 列 Should。
- **文档 A**：spec —— Taro/ADR-0004 有效、RAG 属 Should。
- **代码/ADR 现状**：ADR-0004 被 ADR-0005 推翻；RAG 已成 MVP 核心。
- **裁决（用户：同意）**：以代码/ADR 为准，spec 标注 ADR-0004 已 superseded、前端转原生、RAG 升级为 MVP 核心（已修订）。

---

## 阶段一·第 2 步 文档矛盾裁决（第三批，C1–C2）

### C1 ｜ MVP spec「每日 20 题」vs 代码 `FREE_DAILY_LIMIT=1000`
- **冲突点**：spec 第 92/165/189 行写「免费每日 20 题」；代码常量 `quota.FREE_DAILY_LIMIT = 1000`，且代码内 3 处注释仍残留「20」不自洽。
- **文档 A**：spec —— 20 题。
- **代码现状**：运行时上限 = 1000。
- **裁决（用户：就用 1000，不考虑调回 20）**：以代码为准，`FREE_DAILY_LIMIT=1000` 作为**永久设计**（不做商业化回调）。spec 三处改为 1000；代码 `sessions.py:127` / `quota.py:3` / `schemas.py:166` 注释「20」修正为「1000」。

### C2 ｜ 文档/配置「生产用 MySQL」vs ADR-0010 + RAG 实现要求 PostgreSQL(pgvector)
- **冲突点**：`config.py:7` 注释「生产用 MySQL」、spec 第 117 行「MySQL + 向量库」；但 ADR-0010 明确生产 PG 用 pgvector，RAG 向量检索依赖 PostgreSQL。
- **文档 A**：config / spec —— MySQL。
- **代码/ADR 现状**：ADR-0010 + RAG 实现要求 PostgreSQL（pgvector）。
- **裁决（用户：PostgreSQL）**：生产数据库定为 **PostgreSQL（pgvector）**。修订 `config.py:7` 注释、spec 第 117 行、需求分析相关「MySQL」表述为 PostgreSQL；若需求分析无 MySQL 表述则仅改 config 与 spec。
