# 优师通 · 教资备考垂直领域语言

面向**中小学教师资格考试（NTCE）**备考的 AI 学习产品领域语言：以官方考纲 / 知识点树 / 题型 / 分值为骨架，帮助考生把有限复习时间转化为卷面分。
本项目把「考试内容」「学习过程」「经营规则」三件事分开命名，避免题库与知识库、变式题与真题混用。

> ⚠️ **定位校准（2026-06-07）**：本文件曾按「通用 RAG 出题工具」撰写（原 D7 裁决），该裁决**已撤销**。
> 产品垂直锁定 NTCE 教资备考。「上传任意资料出题」**降级为次要能力**，不再是产品定位主线。
> 现行权威需求口径见 `docs/用户需求文档.md` v2.0。

## 用户与考试

**考生（Candidate）**:
准备参加教师资格证考试并使用本产品刷题的人。
_Avoid_: 用户、学员、同学（「用户」在讨论账号体系时才用）

**学段（Stage）**:
报考类别。**代码里只有三个取值**：幼儿园 / 小学 / 中学 ——
官方口径中的初级中学、高级中学、中职（文化课 / 专业课 / 实习指导）**一律比照中学**：
科目代码同为 301 / 302，差别只在「考不考科目三」，那是报考规则而非学段。
**学段与科目共同决定考纲与模块树**，是所有备考决策的起点。
_Avoid_: 年级、层次
_注_：官方口径中「中职文化课」考 3 科，「中职专业课 / 实习指导」考 2 科（科目三结合面试考查）。

**科目（Subject）**:
笔试科目**序号**：科目一（综合素质）、科目二（因学段而异）、科目三（学科知识与教学能力，仅中学及中职文化课）。
它只是序号，**不含内容** —— 序号 × 学段才唯一确定考纲，那件事由「领域包」承担。
_Avoid_: 课程、学科（「学科」在科目三语境中专指所报教学学科）
_注_：**科目二名称随学段变化**——幼儿园《保教知识与能力》、小学《教育教学知识与能力》、中学《教育知识与能力》。
因此**任何单一名称都不得指代全部学段**（旧代码的 `EDU_KNOWLEDGE` 即此错误，见 ADR-0011）。

**领域包（Domain Pack）**:
由「科目序号 × 学段」**唯一确定**的那份考纲 + 模块树，落成 `backend/app/domain_packs/<目录>/` 一个目录。
**代码里的 `Subject` 枚举就指领域包**：枚举值 = 目录名，于是检索的 `Scope.subject` 可直接当目录名用。
成员数 = **已实现的包数**（当前 2 个：科目一全学段、科目二·中学），**不是官方科目数** ——
官方的 6 个槽位与缺口由 `models.OFFICIAL_SLOTS` 与 `missing_packs()` 表达，
「本仓未实现」不等于「官方不存在」。
_Avoid_: 科目包、学科包、配置包
_注_：**身份**（序号 / 名称 / 覆盖学段 / 试卷代码）的权威在 `models`；
**内容**（模块 / 权重 / 题型 / 考纲版本）在 `pack.yaml`；四条不变量见 ADR-0012 与 `domain_packs/README.md`。

**考期（Exam Date）**:
某一次笔试或面试的官方考试日期，是倒计时与每日任务的时间基准。
_Avoid_: 考试、考季
_注_：笔试每年上、下半年各一次；**笔试单科成绩 2 年有效**，面试须在笔试全部科目合格且成绩有效期内报考。

**模块（Module）**:
题目归属的知识领域。代码 `Module` 枚举含 6 个值：官方考纲五维（职业理念、职业道德、教育法律法规、文化素养、基本能力，各有固定卷面权重）+ `PERSONAL`（个人资料，用户上传资料出题生成的题目归属，掌握度独立成维、不计入官方五维统计、不串入复盘雷达）。
_Avoid_: 章节、分类、板块
_注_: 「官方模块」一律指前五维；任何遍历/统计须显式排除 `PERSONAL`（见 `models.OFFICIAL_MODULES`）。

## 内容

**考点（Knowledge Point）**:
可独立考查的最小知识单元，每道题目归属于且仅归属于一个考点。
_Avoid_: 知识点、考点标签、tag

**题目（Question）**:
一道可作答的题，由题干、选项、正确答案、解析四部分组成（填空/简答可能无选项）。
_Avoid_: 试题、习题、题

**题型（Question Type）**:
代码 `QuestionType` 枚举的**八种**取值，分客观题与主观题两组：
① 客观题五种 —— 单选 `single`、多选 `multiple`、判断 `judge`（复用选项结构，答案为正确/错误）、
填空 `blank`（answer 为可接受答案文本列表，前端归一化匹配）、简答 `short`（answer 为参考答案，不自动判分，标 `self_review`）；
② 主观题三种 —— 材料（案例）分析 `material`、写作 `writing`、教学设计 `design`。
_Avoid_: 题目种类、类别
_注_：主观题只加这三型，因为它们覆盖科目一 **61% 卷面**（材料分析 42 + 写作 50）。
官方题型表（`docs/用户需求文档.md` §1.5）里的其余题型（论述 / 辨析 / 解答 / 课例点评 / 诊断 / 活动设计）
留到真正要做批改时再加 —— 避免先长出一批「有题型、无 rubric」的空壳。

**变式题（Variant）**:
AI 基于某个考点改写或新编的题目，与考纲对齐但不复制任何真题原文。题库的预期主体。
_Avoid_: AI 题、生成题、模拟题

**原题（Authentic Question）**:
官方历年考试真实出现过的题目。本项目**不存储、不展示原题原文**，仅在讨论考频与考点分布时作为分析对象引用。
_Avoid_: 真题（口头可用，正式文档中写「原题」以强调其不可入库）

**知识库（Knowledge Base）**:
生成与校验题目所依据的原始资料集合，分两类：
① **官方知识库**（`Document.is_official`）：官方考纲、教育法条、评分标准（rubric）——**全员可见、可校验**；
② **个人知识库**：考生自主上传的资料——仅本人可见、不经人工审校。
_Avoid_: 语料库、资料库、文档集
_注_：**官方知识库是本产品的语料主体**。备考的核心材料是官方考纲与历年真题（真题因版权不入库），
出题与批改的依据必须来自官方知识库，个人资料出题只是补充场景。

**质量闭环（Quality Loop）**:
出题链路中由 LLM 参与的「检索相关性评分→不达标则查询改写重检索」「生成后自检→不合格则重生成」负反馈机制，带重试上限与降级兜底；用于提升跨文档出题的事实性与考点覆盖。
_Avoid_: 校验循环、自回归

**资料（Document）**:
用户自主上传的文档（PDF / Word / TXT / Markdown），作为出题依据的原始资料，归属上传者本人、不经人工审校。属于知识库范畴，与「原题」一样不进入官方题目池。
_Avoid_: 文件、附件、上传物

**切片（Chunk）**:
资料解析后按语义切分、可独立喂给大模型的文本块，保留顺序、字符数与章节路径等元数据。是**检索的最小单位**，也是喂给模型的最小单位。
_Avoid_: 段落、分块、碎片

**溯源（Provenance / source_chunk）**:
生成题目所依据的资料的切片标识清单，用于把题目回溯到原文（跨文档出题必备）。`Question.source_chunk` 存切片 id 清单；越界或非法时回退为批级清单，防模型幻觉指错。
_Avoid_: 出处、引用

**个人题库（Personal Question Bank）**:
由考生自己上传资料生成的题目集合，**仅本人可见可用**，不进入官方题目池、不经人工审校。与「题库」的区别在于归属与审校状态。
_Avoid_: 我的题、自定义题库、私有题

**题型配比（Question Spec）**:
一次出题请求中用户指定的题型与对应题量（如「单选 10 + 填空 5」），是出题任务的输入契约。
_Avoid_: 出题配置、题目设置

**章节配额（Chapter Quota）**:
整卷出题时按各章节长度比例分配的题量，用于保证题目**覆盖全篇**而非集中在一两章。
_Avoid_: 分配比例、出题权重（「权重」专指模块在卷面的分值占比）

**文档出题任务（DocTask）**:
绑定**单份资料**的异步出题单元（`doc_tasks` 表，`document_id` 非空），含 `paper`（整卷·章节配额分配，覆盖均匀）与 `spot`（定点·Top-K 语义检索，精准命中）两种模式，持有进度与状态，前端轮询。
_Avoid_: 生成任务、作业

**知识库出题任务（KbTask）**:
跨**多份个人资料**按自然语言 `scope` 出题的异步单元（`kb_tasks` 表，仅按 `candidate_id` 跨其资料库），进度/结果落库进程重启不丢，前端轮询后取交付卷。
_Avoid_: 生成任务、作业

**题库（Question Bank）**:
已通过校验与审校、可直接用于作答的题目集合。
_Avoid_: 题集、题目池（「题目池」另有所指，见下）

**离线题目池（Offline Question Pool）**:
预先批量生成并缓存、无需实时调用大模型即可下发的题目库存。与「题库」的区别在于它强调供给方式而非内容状态。
_Avoid_: 缓存题、预生成题库

## 学习过程

**闯关局（Session）**:
一次连续答题的完整过程，固定题量、统一提交，结束后生成复盘报告。
_Avoid_: 一局游戏、一轮、练习

**复盘报告（Review Report）**:
闯关结束后生成的总结，包含掌握度、薄弱考点与下一步建议。
_Avoid_: 学习报告、总结、战报

**掌握度（Mastery）**:
依据答题历史推算出的、考生对某个考点或模块的掌握程度。
_Avoid_: 正确率、熟练度、得分

**错题本（Mistake Book）**:
答错过的题目集合，按考点聚合，是专项重练的入口而非流水账。
_Avoid_: 错题集、错题列表

**每日任务（Daily Task）**:
系统按考期为考生生成的当日学习安排，是留存的主要驱动。**必须真的完成其中的题目**才能标记为完成。
_Avoid_: 打卡、日常任务

**连胜（Streak）**:
连续完成每日任务的自然日数。同一天重复完成**不重复累加**（幂等）。
_Avoid_: 连续打卡、签到天数

**补签卡（Makeup Card）**:
用于补上漏掉的某一天、从而延续连胜的道具。只能通过连胜等**学习行为**获得，上限 3 张。
_Avoid_: 补签券、复活卡（「复活卡」暗示付费道具）

**模考（Mock Exam）**:
按真实考场规则限时组卷的模拟考试：**限时**、答题过程中**不即时判对错**、**统一交卷**后才看结果。
_Avoid_: 模拟卷、全真模拟、考试
_注_：「不可回退」指不能提前知道对错，不禁止回看已答题目。

**批改（Marking）**:
对主观题作答（材料分析题、写作题）给出的结构化评价与改进建议。
_Avoid_: 评分、判卷、纠错

## 质量

**审校（Proofreading）**:
题目在正式进入题库**前**经过的检查，包含结构化校验与人工抽检两步。
_Avoid_: 审核（「审核」留给平台方的小程序审核）、校对

**纠错（Correction Report）**:
考生对已入库题目提交的疑误报告（错误类型：答案/解析/考点归属），进审校队列由最小后台处理（accept/reject）。与「审校」的区别：审校是入库**前**检查，纠错是入库**后**反馈。
_Avoid_: 报错、反馈

## 经营

**每日额度（Daily Quota）**:
非付费考生每天可作答的题目数量上限。按题量限速，不按内容范围设限。
_Avoid_: 免费额度、次数、体力

**实时生成额度（Realtime Quota）**:
每人每日实时大模型生成次数上限（当前 3 次，变式+复盘段落共用），超限自动降级纯池化供给，考生无感。与「每日额度」（作答题量）是两套独立限速。
_Avoid_: 生成次数、调用额度

**VIP** ~~（术语待移除）~~:
⚠️ **B2 裁决（2026-03-29）：产品完全免费，不做 VIP。** 本术语对应的功能与权益已失效；代码侧 VIP 骨架待清理。原定义：付费会员身份，解锁不限量刷题、深度复盘报告与主观题批改。
_Avoid_: 会员、高级用户、Pro
