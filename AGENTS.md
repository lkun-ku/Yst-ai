# AI 闯关学习小程序

微信小程序项目。以下是工程技能的仓库约定。

## Agent skills

### Issue tracker

Issues 以本地 Markdown 文件存放在 `.scratch/<feature-slug>/issues/NN-<slug>.md`。See `docs/agents/issue-tracker.md`.

### Triage labels

使用默认五个标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。See `docs/agents/triage-labels.md`.

### Domain docs

single-context 布局：根 `CONTEXT.md` + `docs/adr/`。See `docs/agents/domain.md`.

## 构建与配置（阶段二固化，#18/#19）

- `project.config.json`：`setting.es6` / `setting.enhance` 已开启，页面源码使用 ESM `import`，
  转译开关须与写法一致，否则换机重装开发者工具即白屏。改动后跑 `node --check` 校验前端语法。
- 内容安全：默认 `CONTENT_SAFETY_MODE=wx`（真实微信 msgSecCheck，见 `app/services/content_safety.py`）。
  生产环境须配置 `WX_APPID` / `WX_SECRET`，缺凭证时自动降级 `stub` 放行，不阻塞本地开发。
- 产品已裁决「完全免费、不做 VIP」：移除 VIP 权益页与 `vip-activate` 分支（`is_vip` 字段保留作向后兼容，恒为 False）。

## 作业纪律

- 单会话作业、改动前 `git stash` / 建分支；修复后跑 `pytest` 全量 + 前端 `node --check`，确认无回归。

## Git 分支约定

### 结构

- `main`：唯一发布主干。任何时刻须可通过 `pytest` 全量与前端 `node --check`。
- `feat/<阶段>-<主题>`：功能分支，**一件事一分支，合并后立即删除**。
- `docs/<主题>`：纯文档 / 预研分支（无代码改动）。
- `fix/<主题>`：临时缺陷修复，用完即删。

> 分支是**指向提交的指针**，不是按模块切分的目录 —— 同一时刻只能检出其一，
> 且所有模块的代码共处同一批文件（`models.py`、`config.py` 同时承载题库/文档/聊天）。
> 因此"按模块建长期分支"不成立；按**工作阶段**建短命分支才是正确用法。

### 功能分支从哪来

按 `docs/改造计划.md` §3 的**分期**逐项建分支，命名 `feat/<主题>`（如 `feat/domain-model`），**合并后立即删除**。

> 此处原先维护过一张「P1–P4 待执行分支表」。**2026-06-10 随边界调整一并作废**：
> 表中那 5 个分支（`feat/p2-official-kb` 等）**从未开工、无独有提交**，已删除。
> 现在**分期以 `docs/改造计划.md` 为单一来源** —— 两处各列一份必然漂移。
>
> 表中仍有效的一条提示保留在此：**官方语料不止考纲 + 法条** ——
> 法条仅占科目一 10% 权重，须同时补写作 / 材料分析 / 教学设计的评分标准（rubric），
> 主观题占 61% 卷面。见 `docs/用户需求文档.md` §5 D1。

### 规则

1. **开工先切分支**：功能改动不直接提交到 `main`。
2. **一事一分支**：便于单独回滚；做坏了直接丢分支，`main` 不受影响。
3. **合并前全绿**：`pytest` 全量 + 前端 `node --check`，否则不合并。
4. **合并后即删**：`git branch -d <branch>`，避免残枝堆积（`chore/pre-github-cleanup` 已按此清理）。
5. **强推需确认**：涉及历史重写的 `push --force-with-lease` 须先确认远端无他人依赖。

## 提交信息约定

### 格式

```
<type>(<scope>): <一句话描述>（#票号）

<正文>
```

- **type**（8 个，取自 Conventional Commits）：`feat` / `fix` / `docs` / `chore` / `refactor` / `test` / `style` / `perf`
- **scope**：**只写模块名，禁止写票号**
- **票号**：本地工单号（`.scratch/<feature>/issues/NN-<slug>.md`）；没有工单可省略，省略时不留空括号
- **多要点**用「——」分隔，不要换行堆要点：
  `feat(chat): 语音模式统一开关——开=按住说话+AI 自动朗读（#38）`
- **正文写「为什么」，不写「做了什么」**：做了什么看 diff 就知道。值得写的是问题是什么、
  为什么这样改、放弃了什么方案（代价与取舍）、怎么验证的（命令 + 数字结论）

### scope 表

| scope | 覆盖范围 |
| --- | --- |
| `mp` | 微信小程序前端（`miniprogram-native/`） |
| `kb` | 知识库：检索 / 出题编排 / 官方语料 |
| `chat` | AI 模拟答 |
| `doc` | 用户上传资料：解析 / 出题 / 原文追溯 |
| `review` | 复盘报告 |
| `sessions` | 闯关局与判分 |
| `admin` | 审校后台 |
| `domain` | 领域模型与领域包：`models` 的科目 / 学段 / 题型、`domain_packs/`、领域相关的 Alembic 迁移 |
| `cleanup` | 仓库整理（非模块，仅整理类提交用） |

> 新增模块时**同步把 scope 补进上表**，否则又会各写各的。

### 为什么定「模块名」而不是票号（实测依据）

改造前实测 120 个提交：`type(scope):` 合规 **119/120（99%）**、带正文 **100/120（83%）**——
约定早已自发成型，不需要重建。但存在一处**漂移**：15 个提交用票号当 scope
（`feat(13): 题目纠错与审校队列…`、`docs(15): UAT验收清单…`），与 `feat(chat)` 这类模块名 scope 混用。

两种口径并存时，`git log --grep` 和 scope 维度的统计都会失真。因为票号已经写在标题末尾的
`（#NN）` 里，scope 位再放一遍是冗余的 —— 故**定为模块名**。

### 工具

- 模板文件：**`.gitmessage`**（随代码入库）
- 启用（**每台机器各执行一次**，属本地配置，不随仓库走）：
  ```bash
  git config commit.template .gitmessage
  ```
- **刻意不引 commitlint**：本仓是 Python + 原生小程序，为一个提交信息规范引入 Node
  工具链（commitlint + husky + node_modules）不划算。模板 + 自觉足够。

### 提交前自检

1. 一个提交只做一件事，能单独 `git revert`
2. 跑过 `python -m pytest` 全量 + 前端 `node --check`
3. 没把 `.env` / 密钥 / 本地库（`backend/dev.db`）/ 实验产物带进来（见 `.gitignore`）

> ⚠️ **本条第 3 项于 2026-06-10 修订**：原文是「没把 `AGENTS.md` / `docs/` 带进来」，
> 该口径**已反转** —— 二者连同 `CONTEXT.md`、`backend/eval/` 已**解除忽略、纳入版本控制**。
> 理由：被忽略会让「归档」与「删除」都无法从 git 恢复（本仓已因此永久丢过一份决策记录）；
> 且 `CONTEXT.md` 是术语的单一真相、`AGENTS.md` 是协作约定，必须随仓库走。
> 仍被忽略的：`.env` / 密钥 / `backend/dev.db` / `backend/uploads/` /
> `backend/eval/eval_kb.db` / `ui-concepts/` / `.scratch/`。
>
> ⚠️ **2026-06-11 二次修订**：`backend/eval/results/` **已解除忽略**（原列在上面那行里）。
> 理由：`results/*.md|json` 是「某项优化到底有没有效」的**唯一证据**，
> 不随代码走就等于没有证据（面试时只能口述数字）。
> 评测库 `eval_kb.db` 仍是本地重算的中间产物，继续忽略。





## 决策协议（人机分工）

**总则**：AI 给材料**也**给推荐，决策由人做。

### 每条决策 AI 提供 4 样

1. **选项**（互斥、尽量穷尽）
2. **判据**（用什么标准判断，**不排序**）
3. **推荐**（写明推荐哪个 + 为什么）
4. **坑**（已知失败模式；**推荐项也必须写代价**，不能只讲优点）

### 人可以怎么回

- 回「**采纳**」= 接受推荐，**无需写理由**
- 回「**选 X**」= 偏离推荐
- **只有偏离推荐时**，AI 回问一句「为什么选它？」 ← 偏离意味着 AI 的分析可能有盲点，值得记录

### AI 的边界

- 偏离推荐时，AI **只挑漏洞，不重新推荐**；人维持原选择则服从并记录
- 决策一旦记录，AI 不得在下游悄悄偏离
- AI 不得用「可点选按钮」代替需要判断的决策

### 变更记录

- v1（2026-06-08）：要求每条决策都写理由 → 摩擦过高，废弃
- v2（2026-06-08 起）：仅「偏离推荐」时要求理由

### 描述客观性（v3 新增）

AI 给选项时，**描述必须中立**。以下一律禁止：

1. ❌ 在选项文字里加评价性符号或词（`✅` / `🔴` / `最优` / `明显更好` / `强烈建议`）
2. ❌ 用带情绪的修饰词（`极端` / `显然` / `真实高频风险` / `致命`）—— 严重度只能在「失效模式」列用统一等级标注
3. ❌ 选项描述长短与详细度不均（有的写 3 行、有的写 6 个字）
4. ❌ 把推荐项的优点混进「判据」陈述里（**判据必须与推荐解耦**）
5. ❌ 按偏好排列选项顺序 —— 顺序规则要固定并预先声明
6. ❌ 只给推荐项写「代价」，其他项不写

**强制对称**：每个选项都必须有 `描述 / 代价 / 失效模式` 三栏，详细度相当。

**顺序规则**：按**成本从低到高**排列（并在开头注明），不按偏好。

**判据与推荐分离**：
- 「判据」只写**判断标准**，不写结论
- 「推荐」单独成段，必须写明：推荐哪个 / 为什么 / **它的代价是什么**



### 通俗讲解（v4 新增，v5 修订）

**问题**：选项里堆生僻名词，看不懂就无法判断；但**每项都展开白话会让内容臃肿，反而拖慢判断**。

**规则：分级展开**

**默认简洁** —— 常见概念直接用标准说法，不展开。
常见词举例（不讲解）：接口、缓存、异步、数据库、配置、日志、超时、重试、并发、版本号、权重、阈值。

**只有这三类才展开「白话 + 比方」**

1. **生僻 / 缩写类**：读者未必见过的术语或缩写（如 CTE、HNSW、RRF、MV3、闭包、拓扑排序）
2. **容易误判类**：同一词在不同语境含义不同，或容易被想当然理解（如「降级」「幂等」「指纹」「断言」）
3. **本方案自造类**：本方案里自定义或重新定义的名词（如「锚点」「风险句」「蓝图模式」「输入指纹」）

**展开格式（仅上述三类）**

**非上述三类**：只写 `描述 + 代价 + 什么时候出问题`，不加白话与比方。

**禁止**
- ❌ 对常见词加白话（如"异步"、"缓存"、"接口"）
- ❌ 只写缩写不解释（缩写属第 1 类，首次出现必须解释；同词后文不重复解释）
- ❌ 只写「代价：中 / 高」这类无信息量的标注

**读者偏好优先**：读者说「这个我懂」后，同类术语不再展开。
