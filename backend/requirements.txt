fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
alembic==1.13.3
pytest==8.3.3
httpx==0.27.2
pypdf==6.18.0
python-docx==1.2.0
numpy==1.26.4

# --- 知识库出题实验（路线③ LangGraph 编排；与 pydantic 2.9.2 / fastapi 0.115 已验证兼容）---
langgraph==1.2.11
langchain-core==1.6.2

# --- 工单 14：PG 生产路径（pgvector）---
# 与 TencentDB PostgreSQL 18.x + pgvector 0.8.x 已实测兼容（2026-03-29 trial）。
# SQLite/dev 不引：保留 LargeBinary 列与内存余弦路径。
psycopg2-binary==2.9.9
pgvector==0.5.0

# --- 领域包（Domain Pack）：科目差异用数据描述，pack.yaml 需 YAML 解析 ---
# 纯数据格式库，不是框架。不引它就只能把领域包写成 JSON（无法写注释）或
# Python 字面量（要求非程序员改代码）—— 而领域包最需要的恰恰是注释。
pyyaml==6.0.1
